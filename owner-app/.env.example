# Both set: the app talks to the real backend through supabase-js and the api Edge Function.
# Both absent: the app runs against the in memory mock (add ?mock=offline|attention|paused|empty|fresh to the URL for alternate states).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
