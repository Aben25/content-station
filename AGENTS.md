# ContentStation

Before working in this repository, read [HANDOFF-PLAN.md](HANDOFF-PLAN.md) for the deployed system, verification evidence, current user direction, operational details and next Postiz milestones. Follow its links when working on the corresponding component.

Keep this checkout focused on the current system: `owner-app/` (web UI), `scripts/clip-lab/` (local clipping UI), `engine-worker/` (shared renderer), `firebase-api/`, `wall-app/` (iPhone camera), and `postiz/` (publishing). Current user instructions, working code and the current handoff determine implementation scope. [docs/README.md](docs/README.md) maps the documentation; [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) maps the code. Superseded prototypes, plans and review reports remain available in Git history; do not restore them as parallel implementations.
